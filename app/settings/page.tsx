import ChatConfig from "@/components/ChatConfig";

const Page = () => {
  
  return (
    <div className="flex items-center justify-center h-screen">
        <ChatConfig />
    </div>
  );
};

export default Page;
